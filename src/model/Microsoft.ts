export interface Iuser {
    username: string;
    accessToken: string;
    mcpSessionId: string;
}

export interface IdataToken {
    bearerToken: string;
    username: string;
    accessToken: string;
}
